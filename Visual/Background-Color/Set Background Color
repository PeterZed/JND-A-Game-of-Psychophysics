function setBackgroundColor() {
  var hue = determinantList.hue.level;
  var saturation = determinantList.saturation.level;
  var lightness = determinantList.lightness.level;
  
  document.body.style.backgroundColor = "hsl(" + hue + ", " + saturation + "%, " + lightness + "%)";
}
