function playGame() {
  changeColor.randomizeColorAttributes(backgroundColor);
  changeColor.setBackgroundColor(backgroundColor);
  
  // For testing, so that clearInterval(colorInterval) can be run as needed
  window.colorInterval = setInterval(changeColor.fluctuateBackgroundColor.bind(changeColor, backgroundColor),
    fluctuationFactors.time.waitTimeMean);
}
